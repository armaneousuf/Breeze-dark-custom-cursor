# BreezeX Cursor

A clone of the main reposotory [ful1e5](https://github.com/ful1e5/BreezeX_Cursor)

Apply in your system:

`yarn render` <br>
`./build.sh` <br>
`cp -r themes/BreezeX-Dark ~/.icons/` (Depends where you copied it)
